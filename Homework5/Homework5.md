HOMEWORK #5: MNIST and NEURAL NETWORKS
1. Building on the previous homework, the goal now is to redo the MNIST classification using a
simple feed forward neural network (FNN). So build such a neural network that maps the input
image to the classifier and hyper parameter tune it to get a reasonably good performance.
2. Extra: After you have tried a feed forward neural network, train a convolutional neural network
(CNN) to classify the MNIST data set. Hyper parameter tune it and compare the results of
the FNN and CNN with your results from the firstr homework.
NOTE: You will write a narrative report about this homework on your github page. Given its
connection with the first homework, simply amend the github page to include the FNN and CNN
structures and scores